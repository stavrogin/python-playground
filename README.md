# Python Playground

A starter "Hello World" project.

## Setup
1. Create venv: `python -m venv venv`
2. Activate: `.\venv\Scripts\activate`
3. Install: `pip install -r requirements.txt`

## Run
`python hello.py`
